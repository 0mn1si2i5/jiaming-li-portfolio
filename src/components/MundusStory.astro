---
import Localized from './Localized.astro';

const base = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
const asset = (path: string) => `${base}${path.replace(/^\//, '')}`;
---

<section class="product-story" aria-labelledby="mundus-story-title">
  <header>
    <p class="eyebrow"><Localized en="Parchment Atlas · Current product" zh="Parchment Atlas · 当前产品" /></p>
    <h2 id="mundus-story-title"><Localized en="One place stays in view." zh="地点始终留在视野中。" /></h2>
    <p><Localized en="The current release supports closer globe inspection, a stable through-Earth section, clear Twilight language, and exact-location sharing with an explicit privacy notice." zh="当前版本支持更近距离观察地球、稳定拖动穿地剖面、清晰呈现曙暮光，并在分享精确地点前给出隐私提示。" /></p>
  </header>

  <figure class="overview-visual">
    <img
      src={asset('/media/mundus/other-side-detail.webp')}
      alt="A close view of the Other Side cross-section and its endpoint city relationship."
      data-alt-en="A close view of the Other Side cross-section and its endpoint city relationship."
      data-alt-zh="“地球另一端”穿地剖面及两端城市关系的细节画面。"
      loading="lazy"
    />
    <figcaption>
      <span><small>01</small><strong><Localized en="Other Side" zh="地球另一端" /></strong><em><Localized en="Spherical relationships" zh="球面空间关系" /></em></span>
      <span><small>02</small><strong><Localized en="Development, Unpacked" zh="发展的不同侧面" /></strong><em><Localized en="Human evidence layers" zh="人类发展证据层" /></em></span>
      <span><small>03</small><strong><Localized en="Sunline" zh="日照线" /></strong><em><Localized en="Time and sunlight" zh="时间与日照变化" /></em></span>
    </figcaption>
  </figure>

  <div class="platform-moment">
    <div class="platform-copy">
      <p class="eyebrow"><Localized en="How it grows" zh="如何继续生长" /></p>
      <h3><Localized en="Each lens joins the same globe." zh="每个视角都接入同一颗地球。" /></h3>
      <p><Localized en="A lens starts with a specific question, pins its data and method, reuses the existing place and controls, and passes the public-build gate." zh="每个视角从具体问题出发，固定数据与方法，复用既有地点与控件，并通过公开构建门禁。" /></p>
      <p id="mundus-extension-label" class="sr-only"><Localized en="Mundus product governance loop" zh="Mundus 产品治理循环" /></p>
      <ol aria-labelledby="mundus-extension-label">
        <li><Localized en="Start with one question" zh="从一个问题出发" /></li>
        <li><Localized en="Pin data and method" zh="固定数据与方法" /></li>
        <li><Localized en="Keep place and controls" zh="保留地点与控件" /></li>
        <li><Localized en="Verify the public build" zh="验证公开构建" /></li>
      </ol>
    </div>
  </div>
</section>

<style>
  .product-story {
    --paper: #151917;
    --ink: #f1eee6;
    --muted: #a8b0aa;
    --line: #414843;
    --signal: #8ec8d7;
    box-sizing: border-box;
    width: 100%;
    margin: 5rem 0;
    padding: clamp(1.5rem, 6vw, 4rem);
    overflow: clip;
    color: var(--ink);
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: clamp(1rem, 3vw, 1.75rem);
  }
  header { max-width: 800px; }
  .eyebrow { margin: 0; color: var(--muted); font-size: .7rem; font-weight: 700; letter-spacing: .12em; text-transform: uppercase; }
  h2, h3 { color: var(--ink); font-weight: 400; line-height: .98; letter-spacing: -.04em; }
  h2 { margin: 1rem 0 1.25rem; font-size: clamp(2.5rem, 6vw, 5rem); }
  h3 { margin: .8rem 0 1rem; font-size: clamp(2rem, 4vw, 3.4rem); }
  header > p:last-child, .platform-copy > p { color: var(--muted); line-height: 1.65; }
  .overview-visual { margin: clamp(2.5rem, 6vw, 5rem) 0 0; }
  .overview-visual img { display: block; width: 100%; height: auto; object-fit: contain; border: 1px solid var(--line); background: #090b0a; }
  .overview-visual figcaption { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); border-bottom: 1px solid var(--line); }
  .overview-visual figcaption > span { display: grid; gap: .25rem; padding: 1rem; border-right: 1px solid var(--line); }
  .overview-visual figcaption > span:last-child { border-right: 0; }
  small { color: var(--signal); font-size: .65rem; letter-spacing: .1em; }
  strong { color: var(--ink); font-size: .9rem; }
  em { color: var(--muted); font-size: .72rem; font-style: normal; }
  .platform-moment { max-width: 760px; margin-top: clamp(4rem, 9vw, 8rem); }
  .platform-copy ol { display: grid; gap: .7rem; margin: 1.5rem 0 0; padding: 0; list-style: none; counter-reset: step; }
  .platform-copy li { display: grid; grid-template-columns: 2rem 1fr; align-items: baseline; padding-top: .7rem; color: var(--ink); border-top: 1px solid var(--line); counter-increment: step; }
  .platform-copy li::before { content: '0' counter(step); color: var(--signal); font-size: .68rem; }
  .sr-only { position: absolute; width: 1px; height: 1px; padding: 0; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0; }
  @media (max-width: 1024px) {
    .overview-visual figcaption { grid-template-columns: 1fr; }
    .overview-visual figcaption > span { border-right: 0; border-bottom: 1px solid var(--line); }
    .overview-visual figcaption > span:last-child { border-bottom: 0; }
  }
  @media (max-width: 760px) {
    .product-story { margin: 4rem 0; }
  }
</style>
